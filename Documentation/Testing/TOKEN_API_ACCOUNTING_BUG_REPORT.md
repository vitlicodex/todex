# Token/API Accounting Bug Report

| ID | Severity | Area | File/line | Bug | Repro fixture | Expected | Actual | Fix | Test coverage | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TDX-ACC-001 | Medium | API transport | `Sources/TokenUsageCore/OpenAIUsageClient.swift` | Redirect handling was delegated to default `URLSession` behavior. | Cross-host or downgrade redirect policy inputs. | Authorization-bearing OpenAI requests should only follow HTTPS same-host/same-port redirects. | Redirect policy was not explicit. | Added `OpenAIRedirectPolicy` and default redirect delegate. | `OpenAI redirect policy` | Fixed |
| TDX-ACC-002 | Medium | Permission accounting | `Sources/TokenUsageCore/CodexPermissionMonitor.swift` | `sandbox_policy.network_access` string values were not parsed by the policy parser. | `restricted`, `disabled`, `enabled` string fixtures. | Restricted/disabled should be false; enabled should be true. | Restricted/disabled became unknown. | Use `networkPolicyValue` for sandbox network access. | `Codex permission sandbox network strings` | Fixed |
| TDX-ACC-003 | Medium | UI/report accounting | `Sources/TokenUsageCore/TokenUsageEngine.swift`; `Sources/TokenUsageMenuBar/*` | Generic cost labels could imply local Codex estimated cost or actual billing interchangeably. | Markdown report generation. | Actual OpenAI API cost labels should be explicit. | Labels used generic daily/monthly cost wording. | Renamed menu/report/header labels. | `Markdown actual API cost labels` | Fixed |
| TDX-ACC-004 | High | API accounting | `Sources/TokenUsageCore/OpenAIUsageClient.swift` | Usage and Costs pagination was not implemented. | Multi-page mocked API response. | All pages should be aggregated with a max-page guard. | Only first page was parsed. | Added bounded cursor pagination for Usage and Costs pages. | `OpenAI Usage API pagination`; `OpenAI Costs API pagination`; `OpenAI pagination max page guard` | Fixed |
| TDX-ACC-005 | High | Partial API failure | `Sources/TokenUsageCore/OpenAIUsageClient.swift` | Partial failure behavior needed endpoint-aware tests. | Usage success + Costs failure; Costs success + Usage failure. | Available data should still display with warning status. | Behavior was partially implemented but under-tested. | Added endpoint-aware mocked fixtures and preserved partial data with warning issues. | `OpenAI pagination partial failure`; `OpenAI usage/costs independent partial failures` | Fixed |
| TDX-ACC-006 | Medium | Local estimated cost | `Sources/TokenUsageCore/TokenUsageModels.swift` | Estimated local cost was not represented in statistics/report models. | Local Codex samples with pricing profile. | Estimated local cost should be separate from actual Costs API values. | Pure estimator existed but was not wired to stats/UI. | Added separate estimated fields, profile labels, report labels, and pricing profile settings. | `Usage store estimated local cost separation`; `Pricing profile cost recalculation`; `UI display model coverage` | Fixed |
